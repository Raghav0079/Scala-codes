// regex - regular expressions - used in pattern matching 
a pattern is specified and the input is checked against that pattern .   match case type problems 
ex - date pattern matching 


// exception handling  -- (try - catch - finally block)
conditions which the compiler cannot handle . runtime errors 
solved using try - catch - finally block 
finally block is not compulsory 
try block - to write the code which may throw an excewption
catch block - to handle the exception 
finally block - to write code whcih is to be executed whether exception ocuurs or not 
when not known what kind of error there is  just use "Exception" keyword

// constructor - a function with name of class name 
gets called when object is initialized 
helps in inheritance for private variables 
// destructor - to refresh the memory after using up of variables 

// single and multilevel are only achivable by default in java and scala
multiple and hierachical ( achivable sometimes ) is achivable in scala nad java
for multiple being achivable   -> interface in java & trait in scala and to access the private vars from parenmt class to child class 

// access modifiers 
to decide what % of data to be accesssed by user , manager and owner
public , private - pkg & espar , protected

// constructors( never private ) - two types -> deafault - ( uses a this :: keyword to call another constructor prevents code from crashing in case of null values) 
                            type casting - changing from one data type to another data type 
                                            types of type casting - 1 . implicit -  done by deafult constructor     
                                                                    2. explicit - done forcefully by user 
                                            if there is type casting i n ML/DL then 90% of time overfitting is there beacuse of scaling up of data ( due to implicit type casting)

                            parametrized- variables can be initialized in the constructor itself and can be used in the class ( saving memory and time)
                                        - also used to craete package of vars and initialize directly in the class 
    - constructor is only defined for a class nothing else
    - constructor can't be defined before function / class definition 
    - also a concept of encapsulation


// abstract - function which can be override 
objects of traits cannot be created directly ( instantiated ) but can be done with help of a class
abstract class cannot be instantiated at all ( no object creation)

// case class - a class used to create objects of a class with the help of a constructor and can be used to create multiple objects of a class with different values ( used in pattern matching )
